/*
==============================================
Typescript definitions for v5.1
==============================================

***********************************************************************
 
This file was automatically generated by Papyrus-2-Typescript.exe
https://github.com/CarlosLeyvaAyala/Papyrus-2-Typescript

The program has no way to know the intention of the humans that made
the scripts, so it's always advisable to manually check all generated
files to make sure everything is declared as it should.
*/

import * as sp from "skyrimPlatform"

import { Alias } from "skyrimPlatform"
import { Form } from "skyrimPlatform"
import { Quest } from "skyrimPlatform"
import { ReferenceAlias } from "skyrimPlatform"

const sn = (sp as any).PO3_Events_Alias

/** EVENTS SHOULD BE CALLED ON AN ALIAS - script that is attached to a reference alias/location alias.
* DOCUMENTATION IS AVAILABLE AT https://github.com/powerof3/PapyrusExtenderSSE/wiki
* ACTOR FALL LONG DISTANCE
*/
	
export const RegisterForActorFallLongDistance = (akRefAlias: ReferenceAlias | null): void => sn.RegisterForActorFallLongDistance(akRefAlias)
export const UnregisterForActorFallLongDistance = (akRefAlias: ReferenceAlias | null): void => sn.UnregisterForActorFallLongDistance(akRefAlias)

//ACTOR KILL
	
export const RegisterForActorKilled = (akAlias: Alias | null): void => sn.RegisterForActorKilled(akAlias)
export const UnregisterForActorKilled = (akAlias: Alias | null): void => sn.UnregisterForActorKilled(akAlias)

//ACTOR REANIMATE
	
export const RegisterForActorReanimateStart = (akAlias: Alias | null): void => sn.RegisterForActorReanimateStart(akAlias)
export const UnregisterForActorReanimateStart = (akAlias: Alias | null): void => sn.UnregisterForActorReanimateStart(akAlias)
	
export const RegisterForActorReanimateStop = (akAlias: Alias | null): void => sn.RegisterForActorReanimateStop(akAlias)
export const UnregisterForActorReanimateStop = (akAlias: Alias | null): void => sn.UnregisterForActorReanimateStop(akAlias)

//ACTOR RESURRECT
	
export const RegisterForActorResurrected = (akAlias: Alias | null): void => sn.RegisterForActorResurrected(akAlias)
export const UnregisterForActorResurrected = (akAlias: Alias | null): void => sn.UnregisterForActorResurrected(akAlias)

//BOOKS READ
	
export const RegisterForBookRead = (akAlias: Alias | null): void => sn.RegisterForBookRead(akAlias)
export const UnregisterForBookRead = (akAlias: Alias | null): void => sn.UnregisterForBookRead(akAlias)

//CELL FULLY LOADED
	
export const RegisterForCellFullyLoaded = (akAlias: Alias | null): void => sn.RegisterForCellFullyLoaded(akAlias)
export const UnregisterForCellFullyLoaded = (akAlias: Alias | null): void => sn.UnregisterForCellFullyLoaded(akAlias)

//CRITICAL HIT
	
export const RegisterForCriticalHit = (akAlias: Alias | null): void => sn.RegisterForCriticalHit(akAlias)
export const UnregisterForCriticalHit = (akAlias: Alias | null): void => sn.UnregisterForCriticalHit(akAlias)

//DISARMED
	
export const RegisterForDisarmed = (akAlias: Alias | null): void => sn.RegisterForDisarmed(akAlias)
export const UnregisterForDisarmed = (akAlias: Alias | null): void => sn.UnregisterForDisarmed(akAlias)

//DRAGON SOUL ABSORBED
	
export const RegisterForDragonSoulGained = (akAlias: Alias | null): void => sn.RegisterForDragonSoulGained(akAlias)
export const UnregisterForDragonSoulGained = (akAlias: Alias | null): void => sn.UnregisterForDragonSoulGained(akAlias)

//ON HIT EX

export const RegisterForHitEventEx = (akRefAlias: ReferenceAlias | null, akAggressorFilter: Form | null = null, akSourceFilter: Form | null = null, akProjectileFilter: Form | null = null, aiPowerFilter: number = -1, aiSneakFilter: number = -1, aiBashFilter: number = -1, aiBlockFilter: number = -1, abMatch: boolean = true): void => sn.RegisterForHitEventEx(akRefAlias, akAggressorFilter, akSourceFilter, akProjectileFilter, aiPowerFilter, aiSneakFilter, aiBashFilter, aiBlockFilter, abMatch)
	
export const UnregisterForHitEventEx = (akRefAlias: ReferenceAlias | null, akAggressorFilter: Form | null = null, akSourceFilter: Form | null = null, akProjectileFilter: Form | null = null, aiPowerFilter: number = -1, aiSneakFilter: number = -1, aiBashFilter: number = -1, aiBlockFilter: number = -1, abMatch: boolean = true): void => sn.UnregisterForHitEventEx(akRefAlias, akAggressorFilter, akSourceFilter, akProjectileFilter, aiPowerFilter, aiSneakFilter, aiBashFilter, aiBlockFilter, abMatch)
	
export const UnregisterForAllHitEventsEx = (akRefAlias: ReferenceAlias | null): void => sn.UnregisterForAllHitEventsEx(akRefAlias)

//ITEM CRAFTED

export const RegisterForItemCrafted = (akAlias: Alias | null): void => sn.RegisterForItemCrafted(akAlias)
export const UnregisterForItemCrafted = (akAlias: Alias | null): void => sn.UnregisterForItemCrafted(akAlias)

//ITEM HARVESTED
	
export const RegisterForItemHarvested = (akAlias: Alias | null): void => sn.RegisterForItemHarvested(akAlias)
export const UnregisterForItemHarvested = (akAlias: Alias | null): void => sn.UnregisterForItemHarvested(akAlias)

//LEVEL INCREASE
	
export const RegisterForLevelIncrease = (akAlias: Alias | null): void => sn.RegisterForLevelIncrease(akAlias)
export const UnregisterForLevelIncrease = (akAlias: Alias | null): void => sn.UnregisterForLevelIncrease(akAlias)

//LOCATION DISCOVERY
	
export const RegisterForLocationDiscovery = (akAlias: Alias | null): void => sn.RegisterForLocationDiscovery(akAlias)
export const UnregisterForLocationDiscovery = (akAlias: Alias | null): void => sn.UnregisterForLocationDiscovery(akAlias)

//OBJECT GRAB/RELEASE

export const RegisterForObjectGrab = (akAlias: Alias | null): void => sn.RegisterForObjectGrab(akAlias)
export const UnregisterForObjectGrab = (akAlias: Alias | null): void => sn.UnregisterForObjectGrab(akAlias)

//OBJECT LOADED/UNLOADED

export const RegisterForObjectLoaded = (akAlias: Alias | null, formType: number): void => sn.RegisterForObjectLoaded(akAlias, formType)
export const UnregisterForObjectLoaded = (akAlias: Alias | null, formType: number): void => sn.UnregisterForObjectLoaded(akAlias, formType)
export const UnregisterForAllObjectsLoaded = (akAlias: Alias | null): void => sn.UnregisterForAllObjectsLoaded(akAlias)

//QUEST START/STOP

export const RegisterForQuest = (akAlias: Alias | null, akQuest: Quest | null): void => sn.RegisterForQuest(akAlias, akQuest)
export const UnregisterForQuest = (akAlias: Alias | null, akQuest: Quest | null): void => sn.UnregisterForQuest(akAlias, akQuest)
export const UnregisterForAllQuests = (akAlias: Alias | null): void => sn.UnregisterForAllQuests(akAlias)

//QUEST STAGE CHANGE

export const RegisterForQuestStage = (akAlias: Alias | null, akQuest: Quest | null): void => sn.RegisterForQuestStage(akAlias, akQuest)
export const UnregisterForQuestStage = (akAlias: Alias | null, akQuest: Quest | null): void => sn.UnregisterForQuestStage(akAlias, akQuest)
export const UnregisterForAllQuestStages = (akAlias: Alias | null): void => sn.UnregisterForAllQuestStages(akAlias)

//SHOUT ATTACK

export const RegisterForShoutAttack = (akAlias: Alias | null): void => sn.RegisterForShoutAttack(akAlias)
export const UnregisterForShoutAttack = (akAlias: Alias | null): void => sn.UnregisterForShoutAttack(akAlias)

//SKILL INCREASE

export const RegisterForSkillIncrease = (akAlias: Alias | null): void => sn.RegisterForSkillIncrease(akAlias)
export const UnregisterForSkillIncrease = (akAlias: Alias | null): void => sn.UnregisterForSkillIncrease(akAlias)

//SOUL TRAP

export const RegisterForSoulTrapped = (akAlias: Alias | null): void => sn.RegisterForSoulTrapped(akAlias)
export const UnregisterForSoulTrapped = (akAlias: Alias | null): void => sn.UnregisterForSoulTrapped(akAlias)

//SPELL LEARNED

export const RegisterForSpellLearned = (akAlias: Alias | null): void => sn.RegisterForSpellLearned(akAlias)
export const UnregisterForSpellLearned = (akAlias: Alias | null): void => sn.UnregisterForSpellLearned(akAlias)

//WEATHER CHANGE

export const RegisterForWeatherChange = (akAlias: Alias | null): void => sn.RegisterForWeatherChange(akAlias)
export const UnregisterForWeatherChange = (akAlias: Alias | null): void => sn.UnregisterForWeatherChange(akAlias)

//MAGIC EFFECT APPLY

export const RegisterForMagicEffectApplyEx = (akRefAlias: ReferenceAlias | null, akEffectFilter: Form | null, abMatch: boolean): void => sn.RegisterForMagicEffectApplyEx(akRefAlias, akEffectFilter, abMatch)
export const UnregisterForMagicEffectApplyEx = (akRefAlias: ReferenceAlias | null, akEffectFilter: Form | null, abMatch: boolean): void => sn.UnregisterForMagicEffectApplyEx(akRefAlias, akEffectFilter, abMatch)
export const UnregisterForAllMagicEffectApplyEx = (akRefAlias: ReferenceAlias | null): void => sn.UnregisterForAllMagicEffectApplyEx(akRefAlias)

//ON WEAPON HIT

export const RegisterForWeaponHit = (akRefAlias: ReferenceAlias | null): void => sn.RegisterForWeaponHit(akRefAlias)
export const UnregisterForWeaponHit = (akRefAlias: ReferenceAlias | null): void => sn.UnregisterForWeaponHit(akRefAlias)

//ON MAGIC HIT

export const RegisterForMagicHit = (akRefAlias: ReferenceAlias | null): void => sn.RegisterForMagicHit(akRefAlias)
export const UnregisterForMagicHit = (akRefAlias: ReferenceAlias | null): void => sn.UnregisterForMagicHit(akRefAlias)

//ON PROJECTILE HIT

export const RegisterForProjectileHit = (akRefAlias: ReferenceAlias | null): void => sn.RegisterForProjectileHit(akRefAlias)
export const UnregisterForProjectileHit = (akRefAlias: ReferenceAlias | null): void => sn.UnregisterForProjectileHit(akRefAlias)
